<Page x:Class="HCB.UI.USub07"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HCB.UI"
      xmlns:uc="clr-namespace:HCB.UI"
      xmlns:page="clr-namespace:HCB.UI"
      xmlns:vm="clr-namespace:HCB.UI"
      mc:Ignorable="d" 
      Title="USub07">

    <!-- 센서 화면 -->
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SYSTEM/Common/Styles/DefaultResource.xaml"/>
                <ResourceDictionary Source="/SYSTEM/Common/Styles/ButtonsAndSwitches.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    <Border Style="{StaticResource Panel.Radius5}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>

            <Border Grid.Column="0" Grid.Row="0" Margin="10">
                <uc:TitleBar Title="센서 제어"/>
            </Border>
            <Grid Grid.Column="0" Grid.Row="1" Margin="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="입력 신호(수정 불가)"/>
                <ListView 
                    Grid.Row="1"
                    ItemsSource="{Binding InputIoItems}"
                    Background="Transparent" BorderThickness="0"
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                    VirtualizingPanel.IsVirtualizing="True"
                    VirtualizingPanel.VirtualizationMode="Recycling">
                    <!-- 행 구분선 & 패딩 -->
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="12,10"/>
                            <Setter Property="BorderBrush" Value="#16233B"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <!-- 각 항목 = SensorIoItemViewModel -->
                        <DataTemplate DataType="{x:Type vm:SensorIoItemViewModel}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- 센서명 -->
                                <TextBlock Text="{Binding Name}"
                                           VerticalAlignment="Center"
                                           Foreground="White"
                                           FontSize="16"/>      

                                <!-- 토글(오른쪽) : 컨트롤 클래스명을 프로젝트에 맞춰주세요 -->
                                <uc:IoPillToggle Grid.Column="1"
                                                 Margin="8,0,0,0"
                                                 IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                                 IsReadOnly="{Binding IsReadOnly}"
                                                 Command="{Binding ToggleCommand}"
                                                 CommandParameter="{Binding}"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

            </Grid>
            
            
            <Grid Grid.Column="1" Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="출력 신호(수정 가능)"/>
                <ListView 
                    Grid.Row="1"
                    ItemsSource="{Binding OutputIoItems}"
                    Background="Transparent" BorderThickness="0"
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                    VirtualizingPanel.IsVirtualizing="True"
                    VirtualizingPanel.VirtualizationMode="Recycling">
                    <!-- 행 구분선 & 패딩 -->
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="12,10"/>
                            <Setter Property="BorderBrush" Value="#16233B"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <!-- 각 항목 = SensorIoItemViewModel -->
                        <DataTemplate DataType="{x:Type vm:SensorIoItemViewModel}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- 센서명 -->
                                <TextBlock Text="{Binding Name}"
                                           VerticalAlignment="Center"
                                           Foreground="White"
                                           FontSize="16"/>

                                <!-- 토글(오른쪽) : 컨트롤 클래스명을 프로젝트에 맞춰주세요 -->
                                <uc:IoPillToggle Grid.Column="1"
                                                 Margin="8,0,0,0"
                                                 IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                                 IsReadOnly="{Binding IsReadOnly}"
                                                 Command="{Binding ToggleCommand}"
                                                 CommandParameter="{Binding}"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

            </Grid>
        </Grid>

    </Border>
    
</Page>
