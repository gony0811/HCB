<UserControl x:Class="HCB.UI.IoDeviceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HCB.UI"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid local:GridHelper.Columns="1*" local:GridHelper.Rows="Auto, 1*, Auto">
        <telerik:HighlightTextBlock Text="I/O Data" FontSize="20" Style="{StaticResource HighlightTextBlockStyle}" Margin="5"></telerik:HighlightTextBlock>
        <telerik:RadGridView ItemsSource="{Binding Device.IoDataList}"         
                SelectedItem="{Binding SelectedIoData}"
                ShowGroupPanel="False"
                Grid.Row="1"
                AutoGenerateColumns="False"
              IsReadOnly="True"
              VirtualizingPanel.IsVirtualizing="True"
              VirtualizingPanel.VirtualizationMode="Recycling"
              EnableRowVirtualization="True"
              ScrollViewer.CanContentScroll="True"
              Margin="5"
              Style="{StaticResource RadGridViewStyle}">
            <telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn Header="Name" DataMemberBinding="{Binding Name}" Width="1*" />
                <telerik:GridViewDataColumn Header="Io Data Type" DataMemberBinding="{Binding IoType}" Width="1*" />
                <telerik:GridViewDataColumn Header="Address" DataMemberBinding="{Binding Address}" Width="1*" />
                <telerik:GridViewDataColumn Header="Index" DataMemberBinding="{Binding Index}" Width="1*" />
                <telerik:GridViewDataColumn Header="Unit" DataMemberBinding="{Binding Unit}" Width="1*" />
            </telerik:RadGridView.Columns>
        </telerik:RadGridView>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <telerik:RadButton Content="데이터 생성" Command="{Binding IoDataCreateCommand}" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>
