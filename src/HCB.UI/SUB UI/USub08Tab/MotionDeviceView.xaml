<UserControl x:Class="HCB.UI.MotionDeviceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HCB.UI"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid local:GridHelper.Columns="1*, 1*" local:GridHelper.Rows="Auto, 1*, Auto">
        <TextBlock Text="Motion" FontSize="20"></TextBlock>
        <DataGrid ItemsSource="{Binding Device.MotionList}"
                  SelectedItem="{Binding SelectedMotion}"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                CanUserAddRows="True"
                IsReadOnly="True"
                HeadersVisibility="Column"
                RowHeaderWidth="0"
                VirtualizingPanel.IsVirtualizing="True"
                VirtualizingPanel.VirtualizationMode="Recycling"
                EnableRowVirtualization="True"
                ScrollViewer.CanContentScroll="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="번호" Binding="{Binding MotorNo}" Width="0.6*"/>
                <DataGridTextColumn Header="이름" Binding="{Binding Name}" Width="1*" />
                <DataGridTextColumn Header="모션 타입" Binding="{Binding ControlType}" Width="1*" />
                <DataGridTextColumn Header="최소 위치" Binding="{Binding LimitMinPosition}" Width="1*" />
                <DataGridTextColumn Header="최대 위치" Binding="{Binding LimitMaxPosition}" Width="1*" />
                <DataGridTextColumn Header="최소 속도" Binding="{Binding LimitMinSpeed}" Width="1*" />
                <DataGridTextColumn Header="최대 속도" Binding="{Binding LimitMaxSpeed}" Width="1*" />
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="모션 생성" Command="{Binding MotionCreateCommand}"/>
        </StackPanel>

        <TextBlock Text="Motion Paramter" Grid.Column="1" FontSize="20" ></TextBlock>
        <DataGrid ItemsSource="{Binding SelectedMotion.ParameterList}"
                  Grid.Column="1"
                    Grid.Row="1"
                    AutoGenerateColumns="False"
                    CanUserAddRows="True"
                    IsReadOnly="True"
                    HeadersVisibility="Column"
                    RowHeaderWidth="0"
                    VirtualizingPanel.IsVirtualizing="True"
                    VirtualizingPanel.VirtualizationMode="Recycling"
                    EnableRowVirtualization="True"
                    ScrollViewer.CanContentScroll="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="이름" Binding="{Binding Name}" Width="1*" />
                <DataGridTextColumn Header="값 타입" Binding="{Binding ValueType}" Width="1*" />
                <DataGridTextColumn Header="값" Binding="{Binding Value}" Width="1*" />
                <DataGridTextColumn Header="단위" Binding="{Binding Unit}" Width="1*" />
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="파라미터 생성" Command="{Binding MotionParameterCreateCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
