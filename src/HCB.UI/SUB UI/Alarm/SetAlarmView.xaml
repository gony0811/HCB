<UserControl x:Class="HCB.UI.SetAlarmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HCB.UI"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Width="750" Height="500" Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#C0392B" Padding="15,0">
            <DockPanel>
                <TextBlock Text="⚠" FontSize="30" Foreground="White" VerticalAlignment="Center" Margin="0,0,15,0"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="CRITICAL SYSTEM ALARM" Foreground="White" FontWeight="ExtraBold" FontSize="18"/>
                    <TextBlock Text="{Binding SelectedAlarm.LastRaisedAt, StringFormat='발생 시간: {0:yyyy-MM-dd HH:mm:ss}'}" 
                               Foreground="#FFDADADA" FontSize="11"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <telerik:RadGridView Grid.Row="1" 
                             ItemsSource="{Binding AlarmList}" 
                             SelectedItem="{Binding SelectedAlarm}"
                             AutoGenerateColumns="False" 
                             IsReadOnly="True"
                             ShowGroupPanel="False"
                             RowIndicatorVisibility="Collapsed"
                             AlternateRowBackground="#2D2D2D"
                             BorderThickness="0"
                             Margin="5">
            <telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn Header="CODE" DataMemberBinding="{Binding Code}" Width="80" TextAlignment="Center"/>
                <telerik:GridViewDataColumn Header="NAME" DataMemberBinding="{Binding Name}" Width="200"/>
                <telerik:GridViewDataColumn Header="LEVEL" DataMemberBinding="{Binding Level}" Width="100"/>
                <telerik:GridViewDataColumn Header="DESCRIPTION" DataMemberBinding="{Binding Description}" Width="*"/>
            </telerik:RadGridView.Columns>
        </telerik:RadGridView>

        <Border Grid.Row="2" Background="#252526" BorderBrush="#C0392B" BorderThickness="2,0,0,0" Margin="10,5" Padding="15">
            <StackPanel>
                <TextBlock Text="💡 조치 방법 (Action Required)" Foreground="#F1C40F" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock Text="{Binding SelectedAlarm.Action}" 
                           Foreground="White" TextWrapping="Wrap" FontSize="13" LineHeight="18"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="15">
            <telerik:RadButton Content="RESET ALL (전체 해제)" Width="150" Height="40" 
                              Background="#D35400" Foreground="White" FontWeight="Bold"
                              Command="{Binding ResetAllCommand}" Margin="5"/>

        </StackPanel>
    </Grid>
</UserControl>
