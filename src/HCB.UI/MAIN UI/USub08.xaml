<Page x:Class="HCB.UI.USub08"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:HCB.UI"
      mc:Ignorable="d">

    <Page.Resources>
        <local:DeviceDetailTemplateSelector x:Key="DeviceDetailTemplateSelector">
            <local:DeviceDetailTemplateSelector.MotionTemplate>
                <DataTemplate>
                    <local:MotionDeviceView/>
                </DataTemplate>
            </local:DeviceDetailTemplateSelector.MotionTemplate>
            <local:DeviceDetailTemplateSelector.IOTemplate>
                <DataTemplate>
                    <local:IoDeviceView/>
                </DataTemplate>
            </local:DeviceDetailTemplateSelector.IOTemplate>
        </local:DeviceDetailTemplateSelector>

        <!-- DataGrid 공통 -->
        <Style x:Key="DeviceDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="Transparent"/>
            <Setter Property="AlternatingRowBackground" Value="#0A1628"/>
            <Setter Property="Foreground" Value="#ECF0F1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#1C3A52"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
            <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        </Style>

        <Style x:Key="DeviceColumnHeader" TargetType="DataGridColumnHeader"
               BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
            <Setter Property="Background" Value="#0A1628"/>
            <Setter Property="Foreground" Value="#5E8BAA"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="#1C3A52"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="10,7"/>
        </Style>

        <Style x:Key="DeviceCell" TargetType="DataGridCell"
               BasedOn="{StaticResource {x:Type DataGridCell}}">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- 디바이스 생성 버튼 -->
        <Style x:Key="CreateButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#ECF0F1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="#0F3D20"
                                BorderBrush="#27AE60"
                                BorderThickness="1"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1A5A30"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#2ECC71"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#082010"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Page.Resources>

    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <!-- ── 상단: Device 목록 ── -->
        <Border Grid.Row="0"
                Background="#0D1B2A" BorderBrush="#1C3A52" BorderThickness="1"
                CornerRadius="8" Margin="6,6,6,3">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <local:HeaderBar Grid.Row="0" Margin="12,10,12,0" Title="Device"/>
                <Rectangle Grid.Row="0" Height="1" Fill="#1C3A52"
                           VerticalAlignment="Bottom" Margin="12,32,12,0"/>

                <DataGrid Grid.Row="1"
                          Style="{StaticResource DeviceDataGrid}"
                          ColumnHeaderStyle="{StaticResource DeviceColumnHeader}"
                          CellStyle="{StaticResource DeviceCell}"
                          ItemsSource="{Binding DeviceList}"
                          SelectedItem="{Binding SelectedDevice, Mode=TwoWay}"
                          PreviewMouseRightButtonDown="DataGrid_PreviewMouseRightButtonDown"
                          Margin="8,6,8,4">
                    <DataGrid.ContextMenu>
                        <ContextMenu DataContext="{Binding PlacementTarget.DataContext,
                                                  RelativeSource={RelativeSource Self}}">
                            <MenuItem Header="연결"    Command="{Binding DeviceConnCommand}"/>
                            <MenuItem Header="연결해제" Command="{Binding DeviceDisConnCommand}"/>
                            <Separator/>
                            <MenuItem Header="수정" Command="{Binding DeviceUpdateCommand}"/>
                            <MenuItem Header="삭제" Command="{Binding DeviceDeleteCommand}"/>
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name"        Binding="{Binding Name}"        Width="*"/>
                        <DataGridTextColumn Header="Device Type" Binding="{Binding DeviceType}"  Width="*"/>
                        <DataGridTextColumn Header="상태"        Binding="{Binding IsConnected}"  Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <Button Grid.Row="2"
                        Content="디바이스 생성"
                        Style="{StaticResource CreateButton}"
                        Command="{Binding DeviceCreateCommand}"
                        HorizontalAlignment="Right"
                        Margin="12,6,12,10"/>
            </Grid>
        </Border>

        <!-- ── 하단: 추가 정보 (ContentControl) ── -->
        <Border Grid.Row="1"
                Background="#0D1B2A" BorderBrush="#1C3A52" BorderThickness="1"
                CornerRadius="8" Margin="6,3,6,6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <local:HeaderBar Grid.Row="0" Margin="12,10,12,0" Title="추가 정보"/>
                <Rectangle Grid.Row="0" Height="1" Fill="#1C3A52"
                           VerticalAlignment="Bottom" Margin="12,32,12,0"/>

                <ContentControl Grid.Row="1"
                                Margin="8,6,8,8"
                                Content="{Binding SelectedDetailViewModel}"
                                ContentTemplateSelector="{StaticResource DeviceDetailTemplateSelector}"/>
            </Grid>
        </Border>
    </Grid>
</Page>
