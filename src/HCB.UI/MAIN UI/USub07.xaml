<Page x:Class="HCB.UI.USub07"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HCB.UI"
      xmlns:uc="clr-namespace:HCB.UI"
      xmlns:page="clr-namespace:HCB.UI"
      xmlns:vm="clr-namespace:HCB.UI"
      mc:Ignorable="d" 
      Title="USub07">

    <Border>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>

            <Border Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" Margin="10">
                <telerik:RadHighlightTextBlock Text="센서 제어" 
                         FontSize="24" 
                         FontWeight="SemiBold" 
                         Style="{StaticResource RadHighlightTextBlockStyle}"/>
            </Border>



            <Grid Grid.Column="0" Grid.Row="1" Margin="0" local:GridHelper.Rows="Auto, 1*">
                <telerik:RadHighlightTextBlock Text="입력 신호 (디지털)" 
                                 FontSize="15" 
                                 FontWeight="SemiBold" 
                                 Style="{StaticResource RadHighlightTextBlockStyle}"/>

                <ListView 
                        Grid.Row="1"
                        ItemsSource="{Binding DigitalInput}"
                        Background="Transparent" BorderThickness="0"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        VirtualizingPanel.IsVirtualizing="True"
                        VirtualizingPanel.VirtualizationMode="Recycling">
                    <!-- 행 구분선 & 패딩 -->
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="12,10"/>
                            <Setter Property="BorderBrush" Value="Gray"/>
                            <Setter Property="BorderThickness" Value="1"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <!-- 각 항목 = SensorIoItemViewModel -->
                        <DataTemplate DataType="{x:Type vm:SensorIoItemViewModel}">
                            <Grid ToolTip="{Binding Description}" Background="Transparent" ToolTipService.InitialShowDelay="100" ToolTipService.BetweenShowDelay="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- 센서명 -->
                                <telerik:HighlightTextBlock Style="{StaticResource HighlightTextBlockStyle}" Text="{Binding Name}"
                                           VerticalAlignment="Center"
                                           Foreground="White"
                                           FontSize="15"/>
                                
                                <uc:IoPillToggle 
                                    Grid.Column="1"
                                    Margin="8,0,0,0"
                                    IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                    IsReadOnly="{Binding IsReadOnly}"
                                    Command="{Binding ToggleCommand}"
                                    CommandParameter="{Binding}"/>
                                
                                <telerik:HighlightTextBlock 
                                    Grid.Column="2"
                                    Margin="10 0 0 0"
                                    Style="{StaticResource HighlightTextBlockStyle}"
                                    Text="{Binding IoName}"
                                    VerticalAlignment="Center"
                                    Foreground="White"
                                    FontSize="15"/>  
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

            <Grid Grid.Column="1" Grid.Row="1" Margin="0" local:GridHelper.Rows="Auto, 1*">
                <telerik:RadHighlightTextBlock Text="출력 신호 (디지털)" 
                                   FontSize="15" 
                                   FontWeight="SemiBold" 
                                   Style="{StaticResource RadHighlightTextBlockStyle}"/>
                <ListView 
                      Grid.Column="1"
                       Grid.Row="1"
                       ItemsSource="{Binding DigitalOutput}"
                       Background="Transparent" BorderThickness="0"
                       ScrollViewer.VerticalScrollBarVisibility="Auto"
                       VirtualizingPanel.IsVirtualizing="True"
                       VirtualizingPanel.VirtualizationMode="Recycling">
                    <!-- 행 구분선 & 패딩 -->
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="12,10"/>
                            <Setter Property="BorderBrush" Value="Gray"/>
                            <Setter Property="BorderThickness" Value="1"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <!-- 각 항목 = SensorIoItemViewModel -->
                        <DataTemplate DataType="{x:Type vm:SensorIoItemViewModel}">
                            <Grid ToolTip="{Binding Description}" Background="Transparent" ToolTipService.InitialShowDelay="100" ToolTipService.BetweenShowDelay="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- 센서명 -->
                                <telerik:HighlightTextBlock Style="{StaticResource HighlightTextBlockStyle}" Text="{Binding Name}"
                                                           VerticalAlignment="Center"
                                                           Foreground="White"
                                                           FontSize="15"/>

                                <!-- 토글(오른쪽) : 컨트롤 클래스명을 프로젝트에 맞춰주세요 -->
                                <uc:IoPillToggle Grid.Column="1"
                                    Margin="8,0,0,0"
                                    IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                    IsReadOnly="{Binding IsReadOnly}"
                                    Command="{Binding ToggleCommand}"
                                    CommandParameter="{Binding}"/>
                                
                                <telerik:HighlightTextBlock 
                                        Grid.Column="2"
                                        Margin="10 0 0 0"
                                        Style="{StaticResource HighlightTextBlockStyle}"
                                        Text="{Binding IoName}"
                                        VerticalAlignment="Center"
                                        Foreground="White"
                                        FontSize="15"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>

    </Border>
    
</Page>
